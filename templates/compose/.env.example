# Copy to .env (git-ignored) and override for local dev.
PROJECT_NAME=chrysa-app
ENV=dev
TAG=local

# App
APP_PORT=8000

# Postgres
POSTGRES_DB=app
POSTGRES_USER=app
POSTGRES_PASSWORD=change-me

# Redis (no password in dev)
